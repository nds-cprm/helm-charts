{{- if .Values.pvc.data.enabled -}}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "geonetwork.fullname" . }}-data
  labels:
    {{- include "geonetwork.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  {{- if .volumeName }}
  volumeName: {{ .volumeName | quote }}
  {{- end }}
  {{- with .Values.pvc.data.storage }}
  storageClassName: {{ .className | quote }}
  resources:
    requests:
      storage: {{ .size }}
  {{- end }}
{{- end }}
